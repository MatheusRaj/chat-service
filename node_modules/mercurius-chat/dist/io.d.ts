/// <reference types="node" />
import * as http from 'http';
import { Server } from 'socket.io';
export declare const httpServer: http.Server;
export declare const initServer: (port: number) => Promise<http.Server>;
export declare const getSocket: (server: http.Server) => Server;
