import { Connection } from './Connection';
import { DefaultChannel } from './DefaultChannel';
import { IMessage } from './interfaces/IMessage';
import { IPublishResult } from './interfaces/IPublishResult';
import { NoExtraProperties } from './types';
export declare class Publisher extends DefaultChannel {
    private topic;
    private options;
    constructor(connection: Connection, topic: string);
    persistent(persist?: boolean): this;
    getConnection(): Connection;
    getTopic(): string;
    send<T = any>(data: NoExtraProperties<IMessage<T>>): Promise<IPublishResult>;
}
