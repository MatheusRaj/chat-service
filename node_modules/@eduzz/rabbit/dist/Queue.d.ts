import { Connection } from './Connection';
import * as amqp from 'amqplib';
export declare class Queue {
    private connection;
    private options;
    private arguments;
    constructor(connection: Connection, name: string);
    topic(...topics: string[]): this;
    durable(durable?: boolean): this;
    disableNack(): this;
    retryTimeout(timeout: number): this;
    ephemeral(): this;
    exclusive(): this;
    prefetch(quantity: number): this;
    priority(max: number): this;
    create(): Promise<void>;
    listen<T>(callback: (data: T, message?: amqp.ConsumeMessage) => Promise<boolean>): Promise<void>;
    private configureNackQueue;
    private configureQueue;
}
