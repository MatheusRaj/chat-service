import { IFallbackAdapter } from '../../interfaces/IFallbackAdapter';
import { IMessage } from '../../interfaces/IMessage';
import { Connection } from '../../Connection';
import { IPublishResult } from '../../interfaces/IPublishResult';
export declare class Memory implements IFallbackAdapter {
    private maxItems;
    private buffer;
    private interval;
    constructor(maxItems?: number);
    store(topic: string, message: IMessage<any>): Promise<IPublishResult>;
    setConnection(connection: Connection): void;
}
